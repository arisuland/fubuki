{
  "name": "arisu",
  "private": true,
  "workspaces": [
    "github-bot/",
    "frontend/",
    "backend/",
    "typings/",
    "locales/"
  ],
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --config --parser typescript ./.prettierrc.json --write backend/src/**/*.ts frontend/src/**/*.{ts,tsx,js,jsx} github-bot/src/**/*.ts typings/src/**/*.d.ts",
    "lint": "yarn workspaces foreach --from '{@arisu/backend,@arisu/frontend,@arisu/github-bot}' -p run lint"
  },
  "devDependencies": {
    "husky": "7.0.1",
    "prettier": "2.3.2"
  }
}
