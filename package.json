{
  "name": "@arisu/root",
  "private": true,
  "workspaces": [
    "github-bot/",
    "typings/",
    "app/",
    "web/"
  ],
  "scripts": {
    "build:ci": "true",
    "prepare": "husky install",
    "fmt": "prettier --config --parser typescript ./.prettierrc.json --write app/src/**/*.ts github-bot/src/**/*.ts typings/typings/**/*.d.ts",
    "build": "true",
    "lint": "yarn workspaces foreach --from '{@arisu/tsubaki,@arisu/github-bot,@arisu/web}' -p run lint"
  },
  "devDependencies": {
    "eslint-config-prettier": "8.3.0",
    "husky": "7.0.2",
    "prettier": "2.4.1",
    "vls": "0.7.6"
  }
}
