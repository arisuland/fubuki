#!/bin/bash

# . "/var/lib/arisu/scripts/liblog.sh"

# . "$PWD/scripts/liblog.sh"

# NODE_VERSION=$(node --version)
# YARN_VERSION=$(yarn --version)
# TSUBAKI_VERSION=$(cat "$PWD/../../app/package.json" | jq .version | tr -d '"')
# FUBUKI_VERSION=$(cat "$PWD/../../web/package.json" | jq .version | tr -d '"')

# info "=========================================================="
# info "|> Welcome to ${BOLD}Arisu${RESET}! Time to get you started..."
# info "|> Using Node.js ${BOLD}$NODE_VERSION${RESET}    | Yarn ${BOLD}v$YARN_VERSION${RESET}"
# info "|> Tsubaki (backend) ${BOLD}v$TSUBAKI_VERSION${RESET} | Fubuki (frontend) ${BOLD}v$FUBUKI_VERSION${RESET}"
# info "=========================================================="
